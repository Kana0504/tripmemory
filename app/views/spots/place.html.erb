<h1>スポット</h1>
<% @spots.each do |t| %>
  <% if t.category == "place" %>  
  <%= t.adress %>
  <%= t.time %>
  <%= t.access %>
  <%= t.parking %>
  <%= t.about %>
  <%= image_tag t.image_url,:class=>"img", size: "250x200" if t.image? %>
  <% end %>
<% end %>